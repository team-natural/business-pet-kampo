---
// SCR-14. Some fields cannot be edited outright — they become a change request the operator
// confirms (F-05-03).
import Layout from "../../layouts/Layout.astro";
import { requireMemberSession } from "../../lib/server/auth/page";

const session = await requireMemberSession(Astro);
if (session instanceof Response) return session;

const organization = session.organization;
---

<Layout title="会社情報 — マイページ">
  <main>
    <h1>会社情報</h1>

    {
      organization ? (
        <dl>
          <dt>会社名</dt>
          <dd>{organization.name}</dd>
          <dt>取引先コード</dt>
          <dd>{organization.orgCode}</dd>
          <dt>取引状況</dt>
          <dd>{organization.status}</dd>
        </dl>
      ) : (
        <p>所属する取引先が見つかりません。お問い合わせください。</p>
      )
    }

    {/* TODO(Phase C): wire to PATCH /api/v1/me/company; fields needing operator confirmation become a change request. */}
    <a href="/mypage">マイページへ戻る</a>
  </main>
</Layout>
