---
// SCR-04. The questions and scoring rules are undecided, so this ships as a coming-soon page and
// the feature is out of the MVP (D-023). Once the questions are settled, add
// packages/content/diagnosis/*.md and replace this with the wizard. The scoring may run entirely
// client-side, but the result must never carry a wholesale price (PRD-02 §2-3).
import Layout from "../../layouts/Layout.astro";

export const prerender = true;
---

<Layout title="商品選び診断">
  <main>
    <h1>商品選び診断</h1>
    <p>ただいま準備中です。公開までは商品一覧から条件を絞ってお探しください。</p>
    <a href="/products">商品一覧を見る</a>
  </main>
</Layout>
